name: my first workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Cheout repo
        uses: actions/checkout@v4
        
      - name: welcome
        run: echo "my first action"

      - name: list files
        run: ls

      - name: Readme file
        run: cat README.md 

      

