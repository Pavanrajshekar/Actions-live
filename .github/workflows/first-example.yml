name: my first workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome
        run: echo "my first action"

      - name: list files
        run: ls

      

